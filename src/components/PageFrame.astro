---
import MobileMenuToggle from 'virtual:starlight/components/MobileMenuToggle';

const { hasSidebar } = Astro.locals.starlightRoute;
---

<div class="page sl-flex">
	<div class="version-banner print:hidden">
		You are viewing the v5 documentation.
		<a href="https://iframe-resizer.com">View the latest v6 docs →</a>
	</div>
	<header class="header"><slot name="header" /></header>
	{
		hasSidebar && (
			<nav class="sidebar print:hidden" aria-label={Astro.locals.t('sidebarNav.accessibleLabel')}>
				<MobileMenuToggle />
				<div id="starlight__sidebar" class="sidebar-pane">
					<div class="sidebar-content sl-flex">
						<slot name="sidebar" />
					</div>
				</div>
			</nav>
		)
	}
	<div class="main-frame"><slot /></div>
</div>

<style>
	@layer starlight.core {
		:global(:root) {
			--version-banner-height: 2.75rem;
			--version-banner-bg: #000;
			--version-banner-fg: #ddd;
		}

		:global(:root[data-theme='dark']) {
			--version-banner-bg: #ddd;
			--version-banner-fg: #333;
		}

		.page {
			flex-direction: column;
			min-height: 100vh;
		}

		.version-banner {
			z-index: calc(var(--sl-z-index-navbar) + 1);
			position: fixed;
			inset-inline-start: 0;
			inset-block-start: 0;
			width: 100%;
			height: var(--version-banner-height);
			display: flex;
			align-items: center;
			justify-content: center;
			gap: 0.5rem;
			padding: 0 1rem;
			background-color: var(--version-banner-bg);
			color: var(--version-banner-fg);
			font-size: var(--sl-text-base);
			font-weight: 600;
			text-align: center;
		}

		.version-banner a {
			color: var(--version-banner-fg);
			text-decoration: underline;
			font-weight: 600;
		}

		.version-banner a:hover {
			text-decoration: none;
		}

		.header {
			z-index: var(--sl-z-index-navbar);
			position: fixed;
			inset-inline-start: 0;
			inset-block-start: var(--version-banner-height);
			width: 100%;
			height: var(--sl-nav-height);
			border-bottom: 1px solid var(--sl-color-hairline-shade);
			padding: var(--sl-nav-pad-y) var(--sl-nav-pad-x);
			padding-inline-end: var(--sl-nav-pad-x);
			background-color: var(--sl-color-bg-nav);
		}

		:global([data-has-sidebar]) .header {
			padding-inline-end: calc(
				var(--sl-nav-gap) + var(--sl-nav-pad-x) + var(--sl-menu-button-size)
			);
		}

		.sidebar-pane {
			visibility: var(--sl-sidebar-visibility, hidden);
			position: fixed;
			z-index: var(--sl-z-index-menu);
			inset-block: calc(var(--sl-nav-height) + var(--version-banner-height)) 0;
			inset-inline-start: 0;
			width: 100%;
			background-color: var(--sl-color-black);
			overflow-y: auto;
		}

		:global([aria-expanded='true']) ~ .sidebar-pane {
			--sl-sidebar-visibility: visible;
		}

		.sidebar-content {
			height: 100%;
			min-height: max-content;
			padding: 1rem var(--sl-sidebar-pad-x) 0;
			flex-direction: column;
			gap: 1rem;
		}

		@media (min-width: 50rem) {
			.sidebar-content::after {
				content: '';
				padding-bottom: 1px;
			}
		}

		.main-frame {
			padding-top: calc(var(--sl-nav-height) + var(--version-banner-height) + var(--sl-mobile-toc-height));
			padding-inline-start: var(--sl-content-inline-start);
		}

		@media (min-width: 50rem) {
			:global([data-has-sidebar]) .header {
				padding-inline-end: var(--sl-nav-pad-x);
			}
			.sidebar-pane {
				--sl-sidebar-visibility: visible;
				width: var(--sl-sidebar-width);
				background-color: var(--sl-color-bg-sidebar);
				border-inline-end: 1px solid var(--sl-color-hairline-shade);
			}
		}

		@media print {
			.header {
				inset-block-start: 0;
			}
			.main-frame {
				padding-top: calc(var(--sl-nav-height) + var(--sl-mobile-toc-height));
			}
		}
	}
</style>
